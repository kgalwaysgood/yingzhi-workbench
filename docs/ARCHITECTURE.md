# 架构说明

影知工坊采用本地优先架构：

1. Windows 原生客户端负责五步工作流和用户交互。
2. 本地 Node 服务负责任务编排、资料库接口和状态管理。
3. 浏览器自动化仅在后台隔离桌面运行；只有用户明确选择交互验证时才允许显示窗口。
4. 下载与解析由固定版本的 MIT 上游组件提供。
5. 转写和总结可以选择本地模型，也可以由用户自行配置兼容 API。
6. Cookie、视频、转写稿、模型和知识成果只保存在用户指定的 D 盘运行目录，不进入源码仓库。

规则化操作由确定性程序完成，模型只用于转写、归纳和知识提炼，不参与权限、安全校验或不可逆决策。
